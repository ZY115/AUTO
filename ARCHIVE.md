# 封存说明

**状态:2026-09-17 停止。** 这份文件是回看这个项目时唯一需要先读的东西。

读法:先看「一分钟版」判断要不要往下读;要查某个具体数字看「实验索引」;
**动手改任何东西之前,先看「撤回台账」**——那里记着哪些结论是错的、错在哪。

所有数字都能在标注的文件里查到。本文件里没有未经核对的引用。

---

## 一分钟版

原问题是「为什么要用时序逻辑」。追下去发现:无论用 TL、DSL 还是别的写法,
最后都会压成一台任务自动机再进 RL。**所以真问题是自动机本身给 RL 带来了什么。**

做完之后的结论,按证据强弱排:

| 结论 | 强度 |
|---|---|
| 未知顺序任务里,发现一点就立刻用,能省 63% 的环境交互 | **强** |
| 省下的几乎全是「重走已知前缀」的路费,不是探索变聪明 | **强** |
| 线性任务不需要自动机,一个进度计数器就够 | **强** |
| 分支任务必须记历史,进度和当前位置都不够 | **强** |
| 前缀树和充分历史字典逐动作一致——价值不在「画成图」 | **强** |
| 自动机和等价手写程序逐种子一致——价值不在语法 | **强** |
| 已知正确的状态划分能大幅减少重复学习 | **强,但有闭式解释** |
| **仅凭有限在线证据可靠地发现该合并哪些历史——没做到** | **这是停下来的原因** |
| 已知结构时,任务路由和技能复用都有收益,量级随条件变 | 中,协议依赖 |

**没有建立任何超过 ISA / QRM 等既有方法的通用新方法。**

---

## 原始问题,以及它后来变成了什么

```
为什么要用 TL？
      ↓  各种写法最后都压成同一台自动机
自动机对 RL 有什么用？
      ↓  那不给自动机呢
能不能边探索边把自动机学出来？
      ↓  简单任务不需要，复杂任务学不可靠
停在这里
```

最后一次组会(2026-09-16)导师没有被说服的那一点,原话是:

> **你说自动机能把很多历史合并,但你凭什么在在线学习过程中知道应该怎么合并?**

这个问题我们跑过测试,**失败了**,见实验索引第 7 项。

---

## 实验索引

三条研究线,不要混读。**每条线只能回答自己那个问题。**

### 线一 · 结构发现(任务规则未知,要边学边用)

#### 1. 立即使用已发现的前缀

`progressive_task_discovery/` · Stage 6 · `results/stage6_early_use/raw.jsonl`

9×9 格子,8 种事件固定在四角和四边中点,起点在正中 (格子 40)。
任务是 k 个事件的隐藏顺序,相邻不重复。**16 个位置只有 8 种事件,所以每种平均出现两次。**
触发正确事件推进一阶段,错误事件被忽略。奖励 = 推进 −0.01 步费。
动作有 20% 概率被随机替换。三个臂每步都是 9 次 Bellman 更新。

| 臂 | 那 9 次更新去哪 | 选动作时读哪张表 |
|---|---|---|
| 普通 replay | 1 在线 + 8 重放,**全进任务表 `Q[阶段][格子][动作]`** | 永远读 `Q` |
| 延迟使用 | 1 进 `Q` + **8 进技能表 `G[事件][格子][动作]`** | 首次完整成功后才准读 `G` |
| 立即使用 | 同上 | 目标一确认就读 `G` |

差别只在一个布尔:`policy_value()` 里的 `method != "delayed_goal" || full_task`。

**结果(k=16,噪声 0.2,32 个种子):**

| | 达到 90% 所需环境步 | 冻结策略完成步数 | 比最优多 |
|---|---:|---:|---:|
| 最优策略(值迭代算出) | — | **169.18** | — |
| 立即使用 | **6,906** | 185.96 | +9.9% |
| 延迟使用 | 37,938 | **173.48** | **+2.5%** |
| 普通 replay | 18,594 | 202.76 | +19.9% |

首次完整成功之前的步数分解:
**已知前缀 延迟 33,194 → 立即 2,566;未知部分 4,218 → 3,741。**
省下的 13 倍全在前缀那一列,探索本身几乎没变。

**必须一起记住的四条限定:**

1. **对外该报 2.7 倍(对普通 replay),不是 5.5 倍(对延迟)。** 延迟臂**没有经验回放**,
   它是「立即臂把读表开关关掉」,任务表每步只更新 1 次而普通 replay 是 9 次,相对普通
   replay 是被结构性削弱的。
2. **探索率不齐:** 立即/延迟 0.005,普通 replay 0.05(开发集选的)。
3. **立即使用的最终策略略差**(+9.9% 对延迟的 +2.5%)。机制:延迟臂被迫游荡顺带给技能表
   做了更广的探索。任务表填充率 立即 55% / 延迟 86%;技能表贪心动作指对方向的比例
   **立即 71.7% / 延迟 98.3%**。六个条件上「游荡时长差」与「策略质量差」相关系数 **−0.856**。
4. **进度是环境免费给的**,不是 agent 推断的(代码里有 `assert(a.stage == env.q)`)。

#### 2. 线性任务不需要自动机

同上。线性序列里进度次数唯一标记阶段,一个「阶段→下一目标」的列表就够。
**所以第 1 项的正结果不能证明一般自动机学习有价值。**

#### 3. 分支任务:必须记历史

`progressive_task_discovery/stage7_branching/`

一个 MDP,**两个真实起点各 50%**,每个起点的唯一出口必须穿过自己的 A 或 B 事件,
两条路线通过实际运动汇合。**不是 multi-task**:一个奖励函数,不需要 task_id,
分支身份藏在 agent 自己的历史里。

走完 `A→E` 和 `B→E` 后进度都是 2、物理位置同一个交叉点、最后事件都是 E,但下一目标一个 D 一个 C。

- 确定性平稳的 count 策略**上限 50%**(此地图此任务此策略类,不是一般上限)
- 有歧义任务:count 和 last-event 基线**所有长度/时限组合 0/32**
- **同地图无歧义控制**(保留起点、走廊、事件、预算、参数、种子、最短路长,只把两条历史的
  后续目标改成相同):**这些基线全部达标** → 失败不是地图太难,是状态表示不充分

#### 4. 表示形式不是贡献

同上。**前缀树与充分历史 dictionary 在匹配输入与随机数时逐动作一致。**
所以价值不来自「画成一张图」。

### 线二 · 状态抽象(哪些历史该合并)

#### 5. 已知正确压缩的价值

`AUTO/docs/MEMORY_COMPRESSION_PILOT.md` · `AUTO/results/runs/memory_compression_pilot_5k.*`

**注意:这是带历史输入的 contextual bandit,不是导航实验。** 一幕 = 依次展示 n 个零件 →
工作台清空 → 选 X 或 Y → 终止。真规则:`A` 在 `B` 前选 X,否则选 Y。一幕只有一次决策。

| 零件数 | 完整历史 T95 | 正确压缩 T95 | 比值 |
|---:|---:|---:|---:|
| 3 | 28.28 | 6.05 | 4.67× |
| 4 | 109.15 | 5.89 | 18.55× |
| 5 | 547.23 | 6.10 | **89.75×** |

**⚠ 这个量级有闭式解释,不是意外发现。** 错误动作 Q 恒为 0,正确动作首次得正奖励后
Q=1 此后永远答对;未覆盖状态随机并列答对一半,故准确率 `(H+c)/2H`,要达 95% 需覆盖
`c ≥ 0.9H`。已覆盖 i 格时新覆盖一格概率 `(H−i)/2H`,期望等待 `2H/(H−i)`,累加得

```
T95 = Σ 2H/(H−i),  i = 0 … ⌈0.9H⌉−1
```

H = n! = 6/24/120 给出 **29.40 / 109.25 / 543.76** 幕,压缩后 H=2 给出 6.0 幕,
**理论倍数 90.63×,实测 89.75×**。大倍数来自填表次数,不需要额外机制假设。

**另一条同样重要:自动机与等价手写程序在 1,200 组上逐种子逐评估点完全一致。**
所以值钱的是状态划分,不是自动机这种语法。

#### 6. 提前用部分结构(同一个 pilot 第二轮)

规则唯一识别前,立即法比延迟法实际多成功 **0.1225 / 0.4525 / 0.635** 次(n=3/4/5)。
**T95 完全相同,而且是结构性必然**:识别前两边都到不了 95%,识别后两边策略一样。

原因:这个任务里**环境每幕独立抽一个新排列,agent 的动作不影响下一份证据**。
而 Stage 6 里行为会改变能走到哪、进而改变拿到什么证据。
**⚠ 但两个实验至少差三条**(主动/被动取证、立即用的是技能调用还是状态合并、
一幕内有无导航),我们没做拆开的对照,**不能断定差异全来自第一条**。

#### 7. 在线判断能不能合并 —— 这条线是这么关掉的

`progressive_task_discovery/stage8_compression/REPORT_STEP35.md` ·
`AUTO/discovery/results/online_occam_n3.json`

n=3 的 8 个任务族,**792 次查询,其中冗余 313 次(39.5%)**。
固定基线轨迹,逐查询只用**当时**的证据回放审计:在「状态数 ≤ k_min + δ」的约束下,
与某条更早历史合并是否被**强制**?被强制就说明这次查询本可以不买。

| 约束 | 被强制的合并 | 裁决正确率 | **归属正确率** | 可省查询 |
|---|---:|---:|---:|---:|
| k_min | 236 | 80.1% | **40.7%** | 16.9% |
| k_min+1 | 22 | 77.3% | 63.6% | 1.8% |
| k_min+2 | 0 | — | — | 0% |

**跑之前写定的判据:查询节省 ≥20% 且错误共享 ≈0。三项全不达标。**
敏感性检验:把 53 次求解超时全判给 Occam,节省上界 23.6%,第二条依然不达标。

**根因:在线 `k_min` 是 8–12,而真实状态数是 17–29。** 用一个小一半的上界必然过度合并——
236 次里 140 次把真状态不同的历史证成了同态。裁决正确率 80.1% 高于归属正确率,
是因为大量错误归属**碰巧**给出相同裁决,这种运气撑不过下一个事件。

另有 102 次复用落在**必要**查询上,那是替换不是节省,未计入 16.9%。

**离线回头看能到 66.1% 覆盖、97.6% 精确率,实时只有 40.7%。**
而且这个界极其脆弱:**多给一个状态余量,覆盖率 66.1% → 6.5%;多给两个 → 0。**

#### 8. 真实压缩比只有 2.16 倍

`AUTO/discovery/results/depth_vs_graph/rungs12.json`

12 个任务族逐族统计:

| | 中位 | 范围 |
|---|---:|---|
| 完整历史状态数 | 46 | 33–77 |
| 真实最小机器状态数 | 22 | 17–29 |
| **压缩比** | **2.16×** | 1.94–2.66 |

**⚠ 「排列组合很多所以需要自动机」这个论证在我们自己的任务上不成立。**
5!=120→2 那个例子来自第 5 项那个 bandit 玩具,不是格子世界。

**哪些固定特征够用(12 个族):**

| 特征 | 充分次数 |
|---|---:|
| `count`(只数推进几步) | **0/12** |
| `bag`(只记发生过哪些事件) | **0/12** |
| `window1` / `window2` | 0/12 |
| `window3` | 3/12 |
| `window4` | 5/12 |
| `window5` | 9/12 |
| **`window6`** | **11/12** |
| `history` | 12/12 |

**每个族的最小充分窗口在 3 到 7 之间。** 这条是停下来时手上最有用的线索:
「只有最近 w 个事件决定未来」这个先验,比「状态数最少」好在三处——它对、它单调安全
(窗口越大越保守)、w 可以从数据估。相关文献:k-reversible languages (Angluin 1982)。

#### 9. 可靠任务反馈的预算

`AUTO/discovery/results/verify.jsonl.gz` · 24 个任务族 × 6 个种子 = 144 次运行

只计量一个问题:「刚才那一步推进任务了吗」。没花钱问过的事件,agent 当它没推进。

| 提问预算 | 0 | 40 | 80 | 160 | 320 |
|---|---:|---:|---:|---:|---:|
| 200,000 环境步内曾达到 90% | **0%** | 1% | 42% | 91% | **100%** |

**预算 0(只有终局奖励)时 0/144 全败,结构树中位节点数 = 1。**
这是「chicken-and-egg」最诚实的回答:我们没有绕过它,我们把它明码标价了。

**⚠ 不能由此推出「结构必须接近完整才能使用」。** `nodes` 字段记的是**历史前缀树**大小
而非压缩状态数,且在运行结束时输出;真正的压缩状态数 `classes` **没有被归档**。

### 线三 · 结构利用(自动机已给定,不涉及边学边建)

#### 10. 路由与复用的 2×2

`progressive_task_discovery/stage8_compression/REPORT_STEP36.md` ·
`results/factorial.json` · 24 族 × 30 种子 × 5 臂,预算 200,000 步

| | 无技能复用 | 有技能复用 |
|---|---|---|
| **无自动机路由** | Y00 平坦历史 | Y01 学习型元控制器 |
| **有自动机路由** | Y10 每状态独立技能 `π(q,g)` | Y11 目标读法 `π(g)` |

QRM(`Q(s,q,a)`,索引读法)不进方格,并排报告。

| 臂 | AUC | first90 中位 | 相对 Y11 |
|---|---:|---:|---:|
| Y00 | .821 | 38,500 | 38.5× |
| Y01 | .883 | 4,000 | 4.0× |
| Y10 | .988 | 3,750 | 3.8× |
| Y11 | .997 | **1,000** | 1.0× |
| QRM | .972 | 7,000 | 7.0× |

**⚠ 三条限定,缺一不可:**

1. **AUC 口径的机制排序已在 Step 38 撤回**(Y11 贴天花板,`Y11−Y10` 只是在量 Y10 掉了多少,
   60 个单元上 corr = −0.998)。报告给出的样本复杂度替代口径(路由 6.39×、复用 5.34×)
   **本次未能从归档复现**(把 `first90 = −1` 记作预算得 9.03×/4.50×),**故不采用**。
2. **删失:** `factorial.json` 里 `first90 = −1`(从未达标)共 40 次,**全部落在 Y00(30/720)
   与 Y01(10/720)**,Y10/Y11/QRM 为 0。上表中位数按达标者计;Y00 若把 −1 记作预算 200,000
   则中位是 41,000 而非 38,500。**该丢弃使 Y00/Y01 显得偏好,所以对比是保守的。**
3. AUC 看起来挤在一起(.821–.997),**实际步数差 38.5 倍**。别用 AUC 排强弱。

#### 11. 不可逆性把差异放大

`progressive_task_discovery/stage8_compression/REPORT_STEP38_5.md`

昂贵导航,危险格数 0→4,预算 1,000,000 环境步:

| 危险格 | 0 | 1 | 2 | 3 | 4 |
|---|---:|---:|---:|---:|---:|
| Y10 分阶段技能 | 100% | 32% | 16% | 11% | 10% |
| Y11 共享技能 | 100% | 100% | 99% | 95% | 91% |

**昂贵地图加岩浆时,「有技能无路由」RMST 0.882 远胜「有路由无技能」0.106。**
**不可逆性放大的是复用,不是路由——与普通环境相反。**

**⚠ 两条限定:** 0.882 是**排除致命目标之后**的数(修好之前是 0.044 / 达标率 5%),
那是路由臂从来不需要的提示;此扫描的**逐种子原始数据已确认遗失**,不能重算区间。
Step 40 新网格显示步费会改变 QRM 排序并缩小风险交互,**不能视为协议无关的效应大小**。

#### 12. 塑形对照:知道顺序 ≠ 知道怎么走最省

`AUTO/discovery/results/depth_vs_graph/` · 12 个 `gap*` 族 × 32 留出种子
**所有臂记忆完全相同,只换塑形信号读哪个距离。**

| 塑形信号 | 达标环境步 | 智能体能算吗 |
|---|---:|---|
| 不塑形 | 29,090 | — |
| 标量进度深度 | **4,324** | 能 |
| 学出来的任务图距离 | 6,164 | 能 |
| **真实任务图距离** | **5,897** | 上帝视角 |
| **真实任务机 × 地图** | **1,128** | 上帝视角 |
| 学出结构 + 就近探索 | **4,273** | 能 |

**两个事实:**

1. **真实任务图(5,897)输给一个标量计数器(4,324)。** 原因是单位不对:任务图数「还欠
   几次转移」,agent 付的是环境步,这些图上物理路程 11–34 步对任务长度 6–13 次转移。
2. **任务机×地图是 1,128,所有可学方法最好 4,273,差 3.7 倍。**

**⚠ 不能读成「计数器这种记忆比自动机好」——这里所有臂的记忆完全相同**,比的是哪个
距离估计更适合当塑形信号。

**这 3.7 倍是停下来时最值得回头挖的东西**,理由见最后一节。

---

## 撤回台账

**回来做任何事之前先读这一节。** 这些结论曾经写进过报告,后来被推翻。
旧报告里可能还留着原话,以本节为准。

| 曾经说过 | 实际 | 出处 |
|---|---|---|
| 「37.15× 加速」 | 达标时刻记在了观察窗**起点**,等于用 900 幕未来信息回溯标记。改记窗口末端后是 **5.04×** | 本会话重算 |
| 0.08× 是 RMST 倍数的交互 | 那是**逐实例比值的几何均值**(统计量 B),不是均值之比(统计量 A)。**A 和 B 是两个量,关于 A 的论证不能套到 B 上** | `analyze_grid.py` |
| 30 个「地图×种子」当独立单位做 bootstrap | 伪重复。重抽单位是**地图**(10 个),不是 30 | 同上 |
| 速度表丢掉未达标实例 | 等于对成功者取条件。改用 RMST + 删失计数 | 同上 |
| 「交互项没有符号」 | 过度修正。加性与乘性交互是不同的量,都合法;**交互方向依赖所选指标与尺度** | — |
| 「只按 C-full 筛选,构造上不可能偏向 Y11」 | 仍是后处理选择 | — |
| 「30 个格子里没有一个偏向 C-full」 | 那是筛选后的子集;全样本是 22 比 8 | — |
| Step 36:路由压倒性强于复用 | **AUC 口径,因天花板已在 Step 38 撤回** | `REPORT_STEP36.md` 顶部 |
| 替代口径 路由 6.39× / 复用 5.34× | **本次未能从归档复现**(−1 记作预算得 9.03×/4.50×) | 本会话重算 |
| 由 `nodes` 推「结构必须接近完整才能使用」 | `nodes` 是**历史前缀树**大小不是压缩状态数,且运行末才输出;`classes` 未归档。**该推断无法由现有数据支持** | `run_verify.py` |
| 「排列组合很多所以需要自动机」 | 真实压缩比只有 **2.16×**;120→2 那个例子来自 bandit 玩具 | `rungs12.json` |
| Step 33 之前两次审计报「零」 | 状态上限填了真实值(19–57),比证据条数(5–40)还大,「每条历史各占一个状态」永远合法。**两次都已撤回** | `REPORT_STEP33.md` |
| 「闭环从来没跑过」 | 假的。`run_verify.py` 用的就是 `--method goal --goal-select learned` | — |
| 「gap* 是 A/B 型简单任务」 | 编的。`gap00.json` 是 `n=3 d=2 m=1 pair=[1,2]`,且 `depth_vs_graph.py` 明写位置+进度计数不可替代历史 | — |
| first90 学出的比给定的慢 11% | **均值比 1.106、中位比 1.250,两个都对**,是两个统计量 | 本会话重算 |
| 「立即使用最终策略和别人一样好」 | 立即 +9.9%,延迟 +2.5%。**立即使用是用最优性换样本效率** | 本会话重算 |

---

## 目录地图

封存时把全部研究产出并进了**一个仓库**:`github.com/ZY115/AUTO`。
克隆它就得到下面这些,不需要别的东西。

```
AUTO/                              ← git: ZY115/AUTO，自包含
├── ARCHIVE.md                      ← 本文件,唯一入口
├── QA.md                            原始问答记录(逐字,未修改)
├── ENVIRONMENT.md                   三套 Python 环境的说明,重跑前必读
│
├── progressive_task_discovery/     【线一 + 线三的主体】
│   ├── REPORT.md                    Stage 6 报告(立即复用)
│   ├── src/pilot.cpp                Stage 6 模拟器,自带单元测试
│   ├── results/stage6_early_use/    含 pilot_snapshot.cpp(跑那批数据的源码)
│   ├── stage7_branching/            分支任务
│   └── stage8_compression/          Step 3–40,40 份逐步报告 + 原始日志
│
├── discovery/                      【线二的主体】
│   ├── src/compress.cpp             压缩与在线合并的模拟器
│   ├── results/                     verify / query / audit / depth_vs_graph
│   └── reports/DECISIONS.md         逐步决策与撤回记录
│
├── docs/                            EXPERIMENT_INDEX / ORIGINAL_QUESTION / 等
├── results/runs/                    神经网格与记忆压缩 pilot
├── tools/                           分析脚本
└── research/                        逐次审计与组会材料,按日期命名
```

**不在这个仓库里的(留在本机,都不是研究产出):**

| | 为什么不收 |
|---|---|
| `external/` 2.8G | 第三方代码(HRM / ISA 等),可重新下载 |
| `tmp/` | 文献 PDF |
| `tl_sequence_pilot/` | 更早的独立项目,自己的仓库 `ZY115/TL` |
| `_superseded/` | 已作废的中途总结,见其中的 README |

编译产物(`pilot`、`compress`、`branch` 等)在 `.gitignore` 里,从源码重建。

**重跑最关键的一条:** `results/stage6_early_use/pilot_snapshot.cpp` 是跑那批数据时的源码。
用它重跑,`trace_hash` 和 `q_hash` 与归档**逐位一致**(本会话验证过三个臂)。
当前 `src/pilot.cpp` 只比它多几条单元测试,无行为差异。

### 封存时做过的两处改动

**一、大日志已 gzip。** 封存时把 5 个原始日志压了(1.2G → 375M,最大的一个 287M → 2.1M)。

```
stage8_compression/results/{generalisation,conversion,conversion_fatal,reward_grid}.jsonl.gz
results/stage5_heldout/raw.jsonl.gz
```

⚠️ **下列脚本按未压缩的文件名读取,跑之前要先 `gunzip`**(故意没改脚本,改了更容易出错):

| 脚本 | 需要的文件 |
|---|---|
| `src/generalisation_audit.py` / `generalisation_report.py` | `generalisation.jsonl` |
| `src/conversion_audit.py` / `conversion_report.py` | `conversion.jsonl` |
| `src/conversion_fatal.py` | `conversion_fatal.jsonl` |
| `src/analyze_reward_grid.py` / `run_reward_grid.py` | `reward_grid.jsonl` |

**二、作废文档移到了 `_superseded/`**,没有删除,那里有一份说明写着每份为什么作废。

---

## 停在哪里

**2026-09-16 组会后停止。** 不是因为实验做得不够,是因为**同一个问题往下走会更乱**。
导师原话:「你先把一个问题弄清楚再往下弄其他的,不然就相当于在一个比较错误的道路上越走越远。」

### 卡住的那个问题

> 凭什么在在线学习过程中知道应该怎么合并两条历史?

**理论上这不是谜。** Myhill–Nerode:`h₁ ~ h₂` 当且仅当对**所有**后缀,未来要求相同。
有限数据永远无法证明等价。只有两条出路:

- **等价查询**(Angluin 的 L*):有 oracle 告诉你反例。**我们没有。**
- **假设一个界**:有了界,有限数据就能强制某些合并。

**我们试的是「状态数上界」(第 7 项),失败了,而且知道为什么:界猜小了一半。**

### 如果回来,从哪接

**优先级一 —— 换先验。** 第 8 项那个结果是现成的:「只有最近 w 个事件决定未来」在
12 个族上 w=6 充分 11 个,而 `count` 和 `bag` 一次都不够。这个先验单调安全、可以从数据估,
而且有文献(k-reversible)。第 7 项的失败诊断直接指向它。

**优先级二 —— 导师自己提的那条,而且不依赖强假设。**
「即使任务顺序已知,分别求 A→B 和 B→C 的最优策略,拼起来也不等于 A→B→C 的最优策略」。
**第 12 项已经有一个 3.7 倍的现成效应量**(真任务机×地图 1,128 对最好可学 4,273)。
这条路的设定正常——**任务顺序已知**,不需要「agent 连自己要干什么都不知道」,
而且直接对上最初那个问题:**显式的任务结构如何改变 RL 的优化问题。**

### 一个不要再犯的表述错误

「两头堵」这个说法(简单任务不需要自动机、复杂任务学不出自动机)**把两件事当成了一根轴**。
第 9 项证明:**任务复杂度完全不变,只改证据通道,可辨识性从 0% 变到 100%。**
可辨识性是**证据通道**的函数,不是任务复杂度的函数。正确的图是两根轴,
而我们站的那一格(复杂任务 + 有价的丰富通道)既不是不需要,也不是学不出来——**它是有价格的**。
