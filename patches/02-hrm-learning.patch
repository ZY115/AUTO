diff --git a/src/ilasp/ilasp_common.py b/src/ilasp/ilasp_common.py
index f98dad6..e60634b 100644
--- a/src/ilasp/ilasp_common.py
+++ b/src/ilasp/ilasp_common.py
@@ -27,4 +27,7 @@ def set_ilasp_env_variables(root_folder_path):
 
 def _set_env_variable(root_folder_path, dir, env_var):
     final_path = os.path.join(root_folder_path, dir)
-    os.environ.putenv(env_var, final_path + os.pathsep + os.environ.get(env_var, ""))
+    # os.environ.putenv was removed in Python 3.9. Assigning to os.environ sets
+    # the variable for this process and its children, which is what the original
+    # call intended; putenv alone never updated os.environ anyway.
+    os.environ[env_var] = final_path + os.pathsep + os.environ.get(env_var, "")
diff --git a/src/reinforcement_learning/ihsa_base_algorithm.py b/src/reinforcement_learning/ihsa_base_algorithm.py
index 264efe7..9198e6c 100644
--- a/src/reinforcement_learning/ihsa_base_algorithm.py
+++ b/src/reinforcement_learning/ihsa_base_algorithm.py
@@ -184,9 +184,16 @@ class IHSAAlgorithmBase(LearningAlgorithm):
         interrupt_episode = False
         hierarchy = self._get_hierarchy(domain_id)
 
-        while not task.is_terminal() and episode_length < self.max_episode_length and not interrupt_episode:
+        while not task.is_terminal() and episode_length < self.max_episode_length \
+                and not interrupt_episode and not self.budget_exhausted:
             action = self._choose_action(domain_id, task_id, current_state, hierarchy, current_hierarchy_state)
             next_state, reward, is_terminal, _ = task.step(action)
+
+            # Environment-step budget. Only real *training* actions count: evaluation
+            # (both the authors' and the dense one) runs with training_enable False,
+            # and broadcast updates are not environment actions at all.
+            if self.training_enable:
+                self._count_train_env_step(domain_id, task_id)
             observation = self._get_task_observation(task)
 
             # update the observation history and get update hierarchy state
@@ -197,6 +204,11 @@ class IHSAAlgorithmBase(LearningAlgorithm):
 
             if self.training_enable:
                 self._update_q_functions(domain_id, task_id, current_state, action, next_state, is_terminal, observation)
+                # Check *after* the update returns, so a broadcasting arm is never
+                # cut off half way through one transition. The loop condition then
+                # ends the episode at the next iteration, which leaves this episode
+                # genuinely incomplete rather than faking a terminal state.
+                self._check_env_step_budget()
 
             # The episode has to be interrupted if an automaton is learnt
             if self._is_counterexample(task, domain_id, next_hierarchy_state):
@@ -222,7 +234,10 @@ class IHSAAlgorithmBase(LearningAlgorithm):
             current_state = next_state
             current_hierarchy_state = next_hierarchy_state
 
-        completed_episode = not interrupt_episode
+        # A budget cut leaves the episode incomplete. `task.is_terminal()` is
+        # reported as it actually is, so an exhausted budget never masquerades as an
+        # environment failure and no terminal flag reaches the last TD update.
+        completed_episode = not interrupt_episode and not self.budget_exhausted
 
         return completed_episode, total_reward, episode_length, task.is_terminal(), observation_history
 
diff --git a/src/reinforcement_learning/ihsa_hrl_algorithm.py b/src/reinforcement_learning/ihsa_hrl_algorithm.py
index 964f47f..97b749a 100644
--- a/src/reinforcement_learning/ihsa_hrl_algorithm.py
+++ b/src/reinforcement_learning/ihsa_hrl_algorithm.py
@@ -22,6 +22,7 @@ class FormulaBank(FormulaTree):
     PSEUDOREWARD_AFTER_STEP = "pseudoreward_after_step"                    # reward given after every step for a formula
 
     FORMULA_UPDATE_SEL_NUM = "formula_update_sel_num"    # number of formula Q-functions to be updated after each step
+    SEPARATE_UPDATE_RNG = "separate_update_rng"          # draw update subsets from a per-bank stream, not the global one
 
     def __init__(self, params, ignore_empty_observations):
         super(FormulaBank, self).__init__()
@@ -38,6 +39,18 @@ class FormulaBank(FormulaTree):
 
         self._formula_update_sel_num = get_param(params, FormulaBank.FORMULA_UPDATE_SEL_NUM, 6)
         self._num_update_calls = 0
+
+        # Which subgoals to update is drawn from NumPy's *global* stream by default,
+        # the same stream the behaviour policy explores from. That makes any change
+        # to update sampling also shift every later exploration draw, so an
+        # experiment that changes the sampling rule is never a single-factor
+        # intervention. Turning this on gives each bank its own generator, leaving
+        # the global stream to the behaviour policy alone.
+        #
+        # Off by default: every result recorded before this existed was produced
+        # with the shared stream, and switching silently would invalidate them.
+        self._separate_update_rng = get_param(params, FormulaBank.SEPARATE_UPDATE_RNG, False)
+        self._update_rng = None
         assert self._formula_update_sel_num is None or self._formula_update_sel_num >= 1
 
         # Keeps track the number of steps for which a subgoal has run, and how many times it has been updated.
@@ -147,7 +160,19 @@ class FormulaBank(FormulaTree):
         update_counters = np.array([self._q_function_update_counter[condition] for condition in base_conditions], dtype=np.float32)
         score = self._num_update_calls + 1 - update_counters  # Use +1 to avoid nullifying probabilities in the following step
         probs = score / np.sum(score)
-        return np.random.choice(base_conditions, self._formula_update_sel_num, replace=False, p=probs)
+        rng = self._update_rng if self._update_rng is not None else np.random
+        return rng.choice(base_conditions, self._formula_update_sel_num, replace=False, p=probs)
+
+    def seed_update_rng(self, seed):
+        """Give this bank its own update-sampling stream.
+
+        Each bank needs a *distinct* seed. The per-state arms build their banks by
+        deep-copying one template, so without re-seeding every copy would carry the
+        same generator state and draw identical subsets — which is shared sampling
+        by accident, exactly the thing the shared-sampling switch is supposed to
+        turn on deliberately."""
+        if self._separate_update_rng:
+            self._update_rng = np.random.default_rng(seed)
 
     @abstractmethod
     def get_q_function(self, formula_condition: FormulaCondition):
@@ -268,6 +293,8 @@ class IHSAAlgorithmHRL(IHSAAlgorithmBase):
 
         # Exploration rates for each subgoal. The step counters for each subgoal are kept in the subclasses.
         self._exploration_rate_subgoal_init = get_param(params, IHSAAlgorithmHRL.EXPLORATION_RATE_PER_SUBGOAL_INIT, 1.0)
+        # None = anneal as published. A number pins epsilon, for controlled checks only.
+        self._subgoal_exploration_rate_const = get_param(params, "subgoal_exploration_rate_const", None)
         self._exploration_rate_subgoal_end = get_param(params, IHSAAlgorithmHRL.EXPLORATION_RATE_PER_SUBGOAL_END, 0.1)
         self._exploration_rate_subgoal_steps = get_param(params, IHSAAlgorithmHRL.EXPLORATION_RATE_PER_SUBGOAL_STEPS, 300000)
 
@@ -542,6 +569,12 @@ class IHSAAlgorithmHRL(IHSAAlgorithmBase):
         return self._option_exploration_base_condition is not None
 
     def _get_formula_exploration_rate(self, task_id, formula_condition: FormulaCondition):
+        # Controlled-acceptance only. The annealing reads the *current bank's*
+        # execution count, so the arms anneal on different counts and their epsilon
+        # schedules drift apart. Pinning it removes that channel, which is what a
+        # stepwise-equivalence check needs; it is never an experiment setting.
+        if self._subgoal_exploration_rate_const is not None:
+            return self._subgoal_exploration_rate_const
         return self._get_annealed_exploration_rate(
             self._get_formula_q_function_step_count(task_id, formula_condition),
             self._exploration_rate_subgoal_init,
diff --git a/src/reinforcement_learning/ihsa_hrl_tabular_algorithm.py b/src/reinforcement_learning/ihsa_hrl_tabular_algorithm.py
index 70467b3..eb70366 100644
--- a/src/reinforcement_learning/ihsa_hrl_tabular_algorithm.py
+++ b/src/reinforcement_learning/ihsa_hrl_tabular_algorithm.py
@@ -92,6 +92,9 @@ class IHSAAlgorithmHRLTabular(IHSAAlgorithmHRL):
             for _ in range(self.num_tasks)
         ]
 
+        for task_id, bank in enumerate(self._formula_banks):
+            bank.seed_update_rng(self.seed_value + 7919 * task_id)
+
         self._automaton_step_counter: Dict[int, Dict[str, Dict[Tuple[str, FormulaCondition], int]]] = {task_id: {} for task_id in range(self.num_tasks)}
         self._init_formula_q_functions()
 
diff --git a/src/reinforcement_learning/learning_algorithm.py b/src/reinforcement_learning/learning_algorithm.py
index 880fb73..897693b 100644
--- a/src/reinforcement_learning/learning_algorithm.py
+++ b/src/reinforcement_learning/learning_algorithm.py
@@ -71,6 +71,10 @@ class LearningAlgorithm(ABC):
 
     REWARD_STEPS_FOLDER = "reward_steps_logs"                # folder where the reward-steps are saved
     REWARD_STEPS_GREEDY_FOLDER = "reward_steps_greedy_logs"  # folder where the reward-steps for the greedy evaluation are saved
+    REWARD_STEPS_DENSE_FOLDER = "reward_steps_dense_logs"    # record-only dense evaluation; never feeds back into training
+    DENSE_EVAL_FREQUENCY = "dense_eval_frequency"            # episodes between record-only evaluations (None = off)
+    DENSE_EVAL_UNTIL = "dense_eval_until"                    # stop the dense evaluation after this many episodes
+    ENV_STEP_BUDGET = "env_step_budget"                      # training environment actions allowed (None = unlimited)
     REWARD_STEPS_FILENAME = "reward_steps-%d.txt"            # reward-steps log file pattern
 
     STATS_SUMMARY_FILENAME = "stats.json"          # name of the file registering the curriculum history
@@ -100,7 +104,14 @@ class LearningAlgorithm(ABC):
 
         # General reinforcement learning parameters
         use_gpu = get_param(params, LearningAlgorithm.USE_GPU, False)
-        self.device = "cuda" if use_gpu and torch.cuda.is_available() else "cpu"
+        # Apple silicon has no CUDA; MPS is the equivalent backend and the
+        # original line would silently fall back to the CPU on this machine.
+        if use_gpu and torch.cuda.is_available():
+            self.device = "cuda"
+        elif use_gpu and getattr(torch.backends, "mps", None) and torch.backends.mps.is_available():
+            self.device = "mps"
+        else:
+            self.device = "cpu"
         self.debug = get_param(params, LearningAlgorithm.DEBUG, False)
         self.training_enable = get_param(params, LearningAlgorithm.TRAINING_ENABLE, True)
         self.num_episodes = get_param(params, LearningAlgorithm.NUM_EPISODES, 20000)
@@ -110,6 +121,25 @@ class LearningAlgorithm(ABC):
         # Greedy evaluation parameters
         self.greedy_evaluation_enable = get_param(params, LearningAlgorithm.GREEDY_EVALUATION_ENABLE, False)
         self.greedy_evaluation_frequency = get_param(params, LearningAlgorithm.GREEDY_EVALUATION_FREQUENCY, 1)
+
+        # Record-only dense evaluation. Added for the C-full probe, where the two
+        # fast arms both sit on the floor of what a 100-episode grid can resolve.
+        # It must not change training, so: the curriculum is never updated from it
+        # (use_curriculum=False), it writes to its own log folder, and the RNG
+        # state is saved and restored around it. Raising the *existing* evaluation
+        # frequency instead would silently change the run, because those results
+        # feed the curriculum sampling distribution.
+        self.dense_eval_frequency = get_param(params, LearningAlgorithm.DENSE_EVAL_FREQUENCY, None)
+        self.dense_eval_until = get_param(params, LearningAlgorithm.DENSE_EVAL_UNTIL, 4000)
+
+        # Environment-step budget. "Same number of episodes" is not "same amount of
+        # environment experience": an episode that dies early and a long successful
+        # one cost very different amounts. Arms that differ in how fast they die
+        # cannot be compared on episode count alone.
+        self.env_step_budget = get_param(params, LearningAlgorithm.ENV_STEP_BUDGET, None)
+        self.budget_exhausted = False
+        self.train_env_steps = 0
+        self.train_env_steps_by_task = {}
         self.greedy_evaluation_episodes = get_param(params, LearningAlgorithm.GREEDY_EVALUATION_EPISODES, 1)
 
         # Trace compression parameters (just copy them from any task)
@@ -138,6 +168,7 @@ class LearningAlgorithm(ABC):
         # Logs for the different tasks
         self.reward_steps_loggers = []
         self.reward_steps_greedy_loggers = []
+        self.reward_steps_dense_loggers = []
 
         # Folder from where the models are loaded/stored
         self.models_folder = os.path.join(
@@ -203,6 +234,9 @@ class LearningAlgorithm(ABC):
         else:
             self._evaluate_greedy_policies()
 
+        if self.training_enable and self.env_step_budget:
+            self._final_frozen_evaluation()
+
         self._write_stats_summary()
 
         if self.training_enable:
@@ -225,6 +259,8 @@ class LearningAlgorithm(ABC):
 
             # Make a checkpoint (the name of the file has the last completed episode)
             current_episode = self._curriculum.get_current_episode()
+            if self.budget_exhausted:
+                self.interrupted_learning = True
             if self.checkpoint_enable and (not completed_episode or (current_episode % self.checkpoint_frequency == 0)):
                 self._make_checkpoint(current_episode)
 
@@ -241,13 +277,17 @@ class LearningAlgorithm(ABC):
                                    self._curriculum.get_current_task(), total_reward, episode_length)
 
         # Needed to log when an automaton is learned (see subclasses)
-        if not completed_episode:
+        # A budget cut is not an automaton-learning interruption: routing it through
+        # _on_incomplete_episode would record a learning event that never happened.
+        if not completed_episode and not self.budget_exhausted:
             self._on_incomplete_episode(self._curriculum.get_current_domain())
 
         # Perform evaluation of the greedy policies
         if self.training_enable and self.greedy_evaluation_enable and self._curriculum.get_current_episode() % self.greedy_evaluation_frequency == 0:
             self._evaluate_greedy_policies()
 
+        self._maybe_dense_evaluate()
+
         # Update domain, task and episode to work with
         if self._curriculum_return_src == "exploratory":
             self._curriculum.update(self._curriculum.get_current_domain(), self._curriculum.get_current_task(), total_reward)
@@ -310,6 +350,70 @@ class LearningAlgorithm(ABC):
     '''
     Greedy Policy Evaluation
     '''
+    def _count_train_env_step(self, domain_id, task_id):
+        self.train_env_steps += 1
+        key = f"{domain_id}:{task_id}"
+        self.train_env_steps_by_task[key] = self.train_env_steps_by_task.get(key, 0) + 1
+
+    def _check_env_step_budget(self):
+        if self.env_step_budget and self.train_env_steps >= self.env_step_budget:
+            self.budget_exhausted = True
+
+    def _final_frozen_evaluation(self):
+        """One last evaluation of the frozen policy, isolated the same way the dense
+        one is: no curriculum update, no training, RNG saved and restored. Written to
+        the authors' greedy log so the curve simply ends with a final point.
+
+        It does not decide attainment. If the ten-point confirmation window is not
+        complete, the analysis reports the run as not having attained, rather than
+        inventing a verdict from a partial window."""
+        import random as _random
+        np_state, py_state = np.random.get_state(), _random.getstate()
+        training_enable = self.training_enable
+        self.training_enable = False
+        try:
+            for domain_id in range(self.num_domains):
+                for task_id in range(self.num_tasks):
+                    self._evaluate_greedy_policies_helper(domain_id, task_id, False, True)
+        finally:
+            self.training_enable = training_enable
+            np.random.set_state(np_state)
+            _random.setstate(py_state)
+
+    def _maybe_dense_evaluate(self):
+        """Record-only evaluation at a finer grain than the authors' schedule.
+
+        Three isolations, each of which would otherwise make this change training:
+          * `use_curriculum=False` so `_curriculum.update` is never called from here;
+          * its own loggers, so the authors' greedy log is untouched;
+          * NumPy and Python RNG state saved and restored, so the extra episodes do
+            not shift the exploration draws of the run that follows.
+
+        If you change the ten-point window used to define attainment, that is a new
+        definition of the attainment time and must not be mixed with the old numbers.
+        """
+        if not self.dense_eval_frequency or not self.training_enable:
+            return
+        episode = self._curriculum.get_current_episode()
+        if episode > self.dense_eval_until or episode % self.dense_eval_frequency != 0:
+            return
+        if episode % self.greedy_evaluation_frequency == 0:
+            return          # the authors' evaluation already covered this episode
+
+        import random as _random
+        np_state, py_state = np.random.get_state(), _random.getstate()
+        training_enable = self.training_enable
+        self.training_enable = False
+        try:
+            for domain_id in range(self.num_domains):
+                for task_id in range(self.num_tasks):
+                    self._evaluate_greedy_policies_helper(
+                        domain_id, task_id, False, True, dense=True)
+        finally:
+            self.training_enable = training_enable
+            np.random.set_state(np_state)
+            _random.setstate(py_state)
+
     def _evaluate_greedy_policies(self):
         # We need this because self.training_enable might be either true or false (it's not always true)
         training_enable = self.training_enable
@@ -323,7 +427,7 @@ class LearningAlgorithm(ABC):
                 self._evaluate_greedy_policies_helper(domain_id, task_id, training_enable, is_domain_active[domain_id])
         self.training_enable = training_enable
 
-    def _evaluate_greedy_policies_helper(self, domain_id, task_id, use_curriculum, is_domain_active):
+    def _evaluate_greedy_policies_helper(self, domain_id, task_id, use_curriculum, is_domain_active, dense=False):
         sum_total_reward, sum_episode_length = 0, 0
         for evaluation_episode in range(self.greedy_evaluation_episodes):
             # If the current domain is available for selection in the curriculum
@@ -349,7 +453,9 @@ class LearningAlgorithm(ABC):
             self._curriculum.update(domain_id, task_id, avg_total_reward)
 
         # TODO: maybe we could also log the std dev for the rewards and steps
-        self._log_reward_and_steps(self.reward_steps_greedy_loggers, domain_id, task_id, avg_total_reward, avg_episode_length)
+        self._log_reward_and_steps(
+            self.reward_steps_dense_loggers if dense else self.reward_steps_greedy_loggers,
+            domain_id, task_id, avg_total_reward, avg_episode_length)
 
     '''
     Logging
@@ -366,6 +472,9 @@ class LearningAlgorithm(ABC):
             if self.greedy_evaluation_enable:
                 self.reward_steps_greedy_loggers = self._init_reward_steps_loggers_helper(LearningAlgorithm.REWARD_STEPS_GREEDY_FOLDER,
                                                                                           LearningAlgorithm.REWARD_STEPS_FILENAME)
+            if self.dense_eval_frequency:
+                self.reward_steps_dense_loggers = self._init_reward_steps_loggers_helper(LearningAlgorithm.REWARD_STEPS_DENSE_FOLDER,
+                                                                                         LearningAlgorithm.REWARD_STEPS_FILENAME)
         else:
             self.reward_steps_greedy_loggers = self._init_reward_steps_loggers_helper(LearningAlgorithm.REWARD_STEPS_GREEDY_FOLDER,
                                                                                       LearningAlgorithm.REWARD_STEPS_FILENAME)
@@ -393,8 +502,16 @@ class LearningAlgorithm(ABC):
         return reward_steps_loggers
 
     def _log_reward_and_steps(self, reward_steps_loggers, domain_id, task_id, episode_reward, episode_length):
-        reward_steps_loggers[domain_id][task_id].info(";".join([str(self._curriculum.get_current_episode()),
-                                                                str(float(episode_reward)), str(episode_length)]))
+        # Fourth field: cumulative *training* environment steps at this evaluation.
+        # Without it a curve can only be read against episode count, and episodes are
+        # not a resource: an episode that dies early and a long successful one cost
+        # very different amounts, so arms that differ in how fast they die cannot be
+        # compared on episodes at all. Readers must accept both 3 and 4 fields, since
+        # every log written before this change has three.
+        reward_steps_loggers[domain_id][task_id].info(";".join([
+            str(self._curriculum.get_current_episode()),
+            str(float(episode_reward)), str(episode_length),
+            str(self.train_env_steps)]))
 
     '''
     Checkpoint Management
@@ -462,7 +579,11 @@ class LearningAlgorithm(ABC):
         self._update_running_time()
         stats = {
             "total_running_time": self.running_time,
-            "interrupted": self.interrupted_learning
+            "interrupted": self.interrupted_learning,
+            "budget_exhausted": self.budget_exhausted,
+            "env_step_budget": self.env_step_budget,
+            "train_env_steps": self.train_env_steps,
+            "train_env_steps_by_task": self.train_env_steps_by_task
         }
 
         if self.training_enable:
diff --git a/src/run_algorithm.py b/src/run_algorithm.py
index 72739e4..f7b6223 100755
--- a/src/run_algorithm.py
+++ b/src/run_algorithm.py
@@ -9,6 +9,9 @@ from reinforcement_learning.drqn_algorithm import DRQNAlgorithm
 from reinforcement_learning.ihsa_crm_algorithm import IHSAAlgorithmCRM
 from reinforcement_learning.ihsa_dqrm_algorithm import IHSAAlgorithmDQRM
 from reinforcement_learning.ihsa_hrl_tabular_algorithm import IHSAAlgorithmHRLTabular
+from reinforcement_learning.ihsa_hrl_tabular_perstate_algorithm import IHSAAlgorithmHRLTabularPerState
+from reinforcement_learning.ihsa_hrl_dqn_perstate_algorithm import IHSAAlgorithmHRLDQNPerState
+from reinforcement_learning.ihsa_hrl_tabular_crossstate_algorithm import IHSAAlgorithmHRLTabularCrossState
 from reinforcement_learning.ihsa_hrl_dqn_algorithm import IHSAAlgorithmHRLDQN
 from reinforcement_learning.learning_algorithm import LearningAlgorithm
 import sys
@@ -72,6 +75,25 @@ if __name__ == "__main__":
                 algorithm = IHSAAlgorithmHRLDQN(config)
             else:
                 raise RuntimeError(f"Error: Unrecognized observation format '{state_format}'.")
+        elif algorithm_name == "ihsa-hrl-perstate":
+            state_format = get_param(config, LearningAlgorithm.STATE_FORMAT)
+            if state_format == LearningAlgorithm.STATE_FORMAT_TABULAR:
+                algorithm = IHSAAlgorithmHRLTabularPerState(config)
+            elif state_format in [LearningAlgorithm.STATE_FORMAT_ONE_HOT, LearningAlgorithm.STATE_FORMAT_FULL_OBS]:
+                algorithm = IHSAAlgorithmHRLDQNPerState(config)
+            else:
+                raise RuntimeError(f"Error: Unrecognized observation format '{state_format}'.")
+        elif algorithm_name == "ihsa-hrl-crossstate":
+            # C-full: per-state parameters like ihsa-hrl-perstate, but every
+            # experience is broadcast to every legal state's bank. Tabular only —
+            # the neural variant would also need the replay-buffer decision
+            # settled, and that is a separate question.
+            state_format = get_param(config, LearningAlgorithm.STATE_FORMAT)
+            if state_format == LearningAlgorithm.STATE_FORMAT_TABULAR:
+                algorithm = IHSAAlgorithmHRLTabularCrossState(config)
+            else:
+                raise RuntimeError(
+                    f"Error: 'ihsa-hrl-crossstate' is tabular only, got '{state_format}'.")
         elif algorithm_name == "ihsa-crm":
             algorithm = IHSAAlgorithmCRM(config)
         elif algorithm_name == "ihsa-dqrm":
